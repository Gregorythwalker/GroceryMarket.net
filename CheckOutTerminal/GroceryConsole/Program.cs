using CheckOutTerminal;

namespace GroceryConsole
{
    class Program
    {
        static void Main(string[] args)
        {
            //main method to call and use class library  
            var view = new View();
            view.Greeting();
        }
    }
}
